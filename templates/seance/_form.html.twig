
<div class="container mt-4">


{{ form_start(form) }}

    <div class="row">
        <div class="col-md-2">{{ form_row(form.jour) }}</div>
        <div class="col-md-2">{{ form_row(form.heure_debut) }}</div>
        <div class="col-md-2">{{ form_row(form.heure_fin) }}</div>
    </div>
    <div class="row">
        <div class="col-md-4">{{ form_row(form.matter) }}</div>
        <div class="col-md-4">{{ form_row(form.teacher) }}</div>
    </div>
    <div class="row">
        <div class="col-md-3">{{ form_row(form.class) }}</div>
        <div class="col-md-3">{{ form_row(form.classroom) }}</div>
    </div>
    {{ form_widget(form) }}

    <div class="container mt-2">
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
        <a class="btn btn-outline-dark" href="{{ path('app_seance_index') }}">back to list</a>
    </div>
</div>
{{ form_end(form) }}
</div>